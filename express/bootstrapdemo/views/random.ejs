<%- include('partials/head')%>
<%- include('partials/navbar')%>
<h1>Your random number is: <%=rand%>
</h1>
<!-- embedding js logic into a template, wrap every line of java script with the special symbol  -->
<% if (rand % 2===0) { %>
    <h2>That is an even number</h2>
    <% } else { %>
        <h2>That is an odd number</h2>
        <% } %>
            <!-- using equal sign to render the strings -->
            <h3> That number is: <%= rand%2===0 ? 'EVEN' : 'ODD' %>
            </h3>
<%- include('partials/footer')%>
